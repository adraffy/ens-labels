{
	"description": "Unique ENS Labels",
	"keywords": [
		"ENS",
		"Ethereum"
	],
	"type": "module",
	"scripts": {
		"sync": "node tool.js sync",
		"text": "node tool.js text",
		"plot": "wolframscript -activate && wolframscript CreatePlot.wls",
		"update": "npm run sync && npm run text && npm run plot"
	},
	"devDependencies": {
		"@adraffy/keccak": "^1.3.0"
	}
}
